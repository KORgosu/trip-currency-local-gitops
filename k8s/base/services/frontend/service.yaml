apiVersion: v1
kind: Service
metadata:
  name: service-frontend
spec:
  selector:
    app: service-frontend
  ports:
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30004
  type: NodePort