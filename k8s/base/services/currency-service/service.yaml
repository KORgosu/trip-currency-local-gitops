apiVersion: v1
kind: Service
metadata:
  name: service-currency
  labels:
    app: service-currency
spec:
  type: ClusterIP
  selector:
    app: service-currency
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP