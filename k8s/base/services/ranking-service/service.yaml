apiVersion: v1
kind: Service
metadata:
  name: service-ranking
  labels:
    app: service-ranking
spec:
  type: ClusterIP
  selector:
    app: service-ranking
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP